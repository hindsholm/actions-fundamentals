name: Hello, World!
description: Greeting with time
author: Morten

inputs:
  who-to-greet:
    description: Who
    required: true
    default: World

outputs:
  time:
    description: Time of greeting

runs:
  using: docker
  image: Dockerfile
  args:
    - ${{ inputs.who-to-greet }}
